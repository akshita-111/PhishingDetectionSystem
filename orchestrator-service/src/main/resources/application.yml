server:
  port: ${PORT:8080}

spring:
  application:
    name: orchestrator-service
  
  data:
    mongodb:
      uri: ${MONGODB_URI:${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/phishing_detection}}

# Brain API Configuration
brain:
  api:
    url: ${BRAIN_API_URL:https://akshita118-brain-api.hf.space/predict}

# Logging Configuration
logging:
  level:
    com.phishing.orchestrator: INFO
    org.springframework.web.client: DEBUG
