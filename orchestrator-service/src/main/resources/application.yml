server:
  port: ${PORT:8080}

spring:
  application:
    name: orchestrator-service
  
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/phishing_detection}

# Brain API Configuration
brain:
  api:
    url: ${BRAIN_API_URL:http://localhost:8000/predict}

# Logging Configuration
logging:
  level:
    com.phishing.orchestrator: INFO
    org.springframework.web.client: DEBUG
